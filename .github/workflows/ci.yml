name: Commitizen and Git Actions

on:
  push:
    branches:
      - main

jobs:
  commitizen:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # - name: Setup SSH
    #   run: |
    #     which ssh-agent || ( apt-get update -qy && apt-get install openssh-client -qqy )
    #     eval `ssh-agent -s`
    #     echo "${SSH_PRIVATE_KEY}" | tr -d '\r' | ssh-add - > /dev/null
    #     mkdir -p ~/.ssh
    #     chmod 700 ~/.ssh
    #     echo "$SSH_PUBLIC_KEY" >> ~/.ssh/id_rsa.pub
    #     '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'

    - name: Install Commitizen
      run: pip3 install -U Commitizen

    # - name: Configure Git
    #   run: |
    #     exists=`git show-ref refs/heads/test_cicd_3`
    #     if [ -n "$exists" ]; then git branch -D test_cicd_3; fi

    - name: Create and push new branch
      run: |
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git checkout -b test_cicd_3
        git fetch --tags
        cz bump --yes
        git branch --set-upstream-to=origin/test_cicd_3 test_cicd_3
        git push origin test_cicd_3:$GITHUB_REF
        TAG=$(head -n 1 VERSION)
        echo "export TAG='$TAG'" >> variables
        git push origin $TAG
